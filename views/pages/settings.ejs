<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <script type="text/javascript" src="dhtml/index.js"></script>
    <script type="text/javascript" src="scripts/settings.js"></script>
  </head>
  <body>
    <!--If logged in, send the HTML for user menu. If not, send HTML for login menu. -->
    <% if(loggedIn) { %> <% include ../partials/menu-overlay %> <% } else { %>
    <% include ../partials/login-overlay %> <% } %>

    <header>
      <% include ../partials/header ) %>
    </header>
    <nav id="main-navigation">
      <% include ../partials/mainNav %>
    </nav>
    <main>
      <button id="shopping-bag-btn">
        <img src="../images/shopping-bag.png" />
        <span>0</span>
      </button>
      <h1><%= fname + " " + lname %></h1>
      <h2>Update account settings</h2>
      <p id="email-exists"></p>
      <form id="settings-form" action="/update-settings" method="post">
        <article>
          <input type="text" name="fname" value=<%= fname%> /><span></span>
        </article>
        <article>
          <input type="text" name="lname" value=<%= lname%> /> <span></span>
        </article>
        <article>
          <input type="text" name="email" value=<%= email%> /> <span></span>
        </article>
        <article>
          <input type="password" name="password" placeholder="New password" />
          <span></span>
        </article>
        <button id="confirm-settings-btn">Confirm</button>
      </form>
    </main>
    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
