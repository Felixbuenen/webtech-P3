<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <script type="text/javascript" src="dhtml/index.js"></script>
  </head>
  <body>
    <!--If logged in, send the HTML for user menu. If not, send HTML for login menu. -->
    <% if(loggedIn) { %> <% include ../partials/menu-overlay %> <% } else { %>
    <% include ../partials/login-overlay %> <% } %>

    <header>
      <% include ../partials/header ) %>
    </header>
    <nav id="main-navigation">
      <% include ../partials/mainNav %>
    </nav>
    <main>
      <button id="shopping-bag-btn">
        <img src="../images/shopping-bag.png" />
        <span>0</span>
      </button>
      <h1><%= fname + " " + lname %></h1>
      <h2>Update account settings</h2>
      <form action="/update-settings" method="post">
        <input type="text" name="fname" value=<%= fname%> /> <input type="text"
        name="lname" value=<%= lname%> /> <input type="text" name="email"
        value=<%= email%> />
        <input
          type="password"
          name="password"
          placeholder="New password"
        /><br />
        <button>Confirm</button>
      </form>
    </main>
    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
